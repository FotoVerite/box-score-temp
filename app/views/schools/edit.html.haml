%h1 #{@school.name} - Edit Profile
.five.columns.centered
  = form_for @school do |f|
    = f.label :name
    = f.text_field :name
    = f.label :mascot
    = f.text_field :mascot
    = f.label :assn
    = f.select :assn, associations

    = f.submit 'Submit', class: 'medium secondary button'
