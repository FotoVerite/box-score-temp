%h1 #{@school.name} - Edit Profile
.five.columns.centered
  = simple_form_for @school do |f|
    = f.input :name
    = f.input :mascot
    = f.input :assn_id, collection: Assn.uniq, label: 'Association', include_blank: false
    = f.input :league_id, collection: League.uniq, label: 'League', include_blank: false

    = f.button :submit, class: 'medium secondary button'
