<% content_for :title, @page.title %>
<h1><%= @page.title %></h1>
<div class="twelve columns" id="main">
  <%= markdown_content(@page.content) %>
  <% if @page.show_last_updated? %>
    <p>Last Updated: <%= @page.updated_at.strftime('%m/%d/%Y') %></p>
  <% end %>
</div>