- if games.length > 0
  %h3= sport
  .row
    - games.each do |game|
      .four.columns
        %table.game-stats
          %thead
            %tr
              %th Team
              %th 1
              %th 2
              %th 3
              %th 4
              %th F

          %tbody
            = render 'games/basketball/game_stats_row', game: game, team: game.away_team
            = render 'games/basketball/game_stats_row', game: game, team: game.home_team

        = link_to 'Box Score', game_path(game), class: 'box-score'
