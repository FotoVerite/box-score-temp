<% if superadmin_signed_in? || post.admin_id == current_admin.try(:id) %>
  <div class="post-actions">
    <%= link_to 'Edit Post', edit_post_path(post) %>
    <%= link_to 'Delete Post', post_path(post), method: :delete, confirm: "Are you sure?" %>
  </div>
<% end %>
